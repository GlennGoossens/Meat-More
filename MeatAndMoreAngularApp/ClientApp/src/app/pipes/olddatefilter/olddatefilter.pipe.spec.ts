import { OlddatefilterPipe } from "./olddatefilter.pipe";

describe("OlddatefilterPipe", () => {
  it("create an instance", () => {
    const pipe = new OlddatefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
